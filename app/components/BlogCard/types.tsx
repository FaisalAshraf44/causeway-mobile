import { StyleProp, ViewStyle } from 'react-native';

export interface Props {
  image: any;
  title: string;
  description: string;
  time: string;
  estimatedReadingTime: any;
  extraWidth?: number;
}
